name: Mantener Koyeb Despierto

on:
  schedule:
    - cron: '*/20 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hacer ping a la app de Delivery
        run: curl -s https://neutral-billie-drkbot-872cd981.koyeb.app/ping
        
